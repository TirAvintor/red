apiVersion: cdi.kubevirt.io/v1beta1
kind: DataVolume
metadata:
  name: windows11-vm-disk
  namespace: centricidiameter-dev
spec:
  source:
    blank: {}   # <-- порожній диск
  pvc:
    accessModes:
      - ReadWriteMany
    resources:
      requests:
        storage: 50Gi
    storageClassName: efs-sc
    volumeMode: Filesystem
