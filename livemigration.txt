apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: windows11-vm-disk
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 64Gi
  storageClassName: efs-sc  # e.g., "standard" or "hostpath-provisioner"
  volumeMode: Block
